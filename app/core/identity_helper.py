"""
Identity helper module — DEPRECATED.

The ZNBNQ user ID is now assigned at registration time via
app/core/validators.py:generate_znbnq_id(). No ID migration is needed.

This module is kept as a stub for backward-compatible imports only.
"""
